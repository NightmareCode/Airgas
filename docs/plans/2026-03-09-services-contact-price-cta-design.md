## Goal
Make “Contact for Price” on the Services page a clear call-to-action that routes users to the Contact form.

## Scope
- Services page only.
- Replace the “Contact for Price” label shown in each gas card with a clickable button.
- Button links to the Contact page form section.

## Approach (Option A)
- Update each gas row price area to render a button linking to `contact.html#contact-form`.
- Keep existing layout and styling patterns (`.btn`, `.btn-primary`) for consistency.

## Success Criteria
- Every gas “Contact for Price” element is clickable.
- Clicking takes the user to the Contact form section.
- Layout remains aligned inside the existing gas visual column.
